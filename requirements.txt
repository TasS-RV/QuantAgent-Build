# Core LLM + agent frameworks
openai>=2.0
openai-agents>=0.17          # OpenAI Agents SDK backtest stack (quant_agents.py / backtest.py)
anthropic
python-dotenv>=1.0
pydantic>=2.0

# LangChain / LangGraph pipeline (trading_graph.py, indicator/trend/pattern agents)
langchain
langchain-core
langchain-openai
langchain-anthropic
langchain-qwq
langchain-google-genai
langgraph

# Google Gemini (AlgoEdge indicator node + LangChain Gemini provider)
google-genai

# Market data + indicators
yfinance>=1.4
pandas>=2.2
numpy>=1.26
pandas-ta>=0.4
TA-Lib
scipy>=1.13

# Charting / visualisation
matplotlib>=3.9
mplfinance>=0.12
plotly
Pillow>=10

# Web UI + utilities
flask
requests
ipython
tqdm

# Optional: required on systems missing a stdlib _sqlite3 build (yfinance caches via sqlite)
pysqlite3-binary

# Optional: VectorBT backend for validate_vectorbt.py (issue #4). Heavy (numba); the
# script falls back to the built-in deterministic engine if this is not installed.
# vectorbt
